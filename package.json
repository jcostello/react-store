{
  "name": "react-store",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "server": "yarn --cwd server dev",
    "app": "yarn --cwd app start",
    "dev": "concurrently run \"yarn server\" \"yarn app\"",
    "cypress": "yarn --cwd app cypress open",
    "test": "yarn --cwd server test"
  },
  "devDependencies": {
    "bson-objectid": "^1.3.0",
    "concurrently": "^5.0.2",
    "faker": "^4.1.0",
    "rosie": "^2.0.1"
  }
}
